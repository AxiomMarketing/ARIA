title: ARIA — AI-Readable Intent Architecture
description: A formal specification language for AI-driven development
url: https://axiommarketing.github.io
baseurl: /ARIA

# Project page deployment:
# - URL: https://axiommarketing.github.io/ARIA/
# - All Liquid links use {{ '/path' | relative_url }} which prepends baseurl
# - Pure HTML files use relative paths (no baseurl injection)

# Jekyll settings
markdown: kramdown
highlighter: rouge
kramdown:
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    block:
      line_numbers: false

# Do not process these files
exclude:
  - README.md
